Admin project (minimal)

This folder is a minimal Expo project scaffold to build the Admin app independently.

To run:

1. cd Client/admin
2. npm install
3. npm run web

You can copy components from the main `Client/app/components` folder or implement admin-specific components here.